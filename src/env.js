/**
 * @module env.js
 * Workaround for create-react-app webpack config & env variables, to be used like dotenv
 */

export default {
  AI_DEBUG_ON: false,
  ENGINE_DEBUG_ON: false,
  FRONTEND_DEBUG_ON: false,
};
